  function render(div: HTMLDivElement, config: object): void {
    // const bookmarksConfig = this.validateConfig(config);
    // console.log('Rendering Bookmarks', bookmarksConfig);
    // for (let bookmark of bookmarksConfig.bookmarks) {
    //   const imgEl = document.createElement('img');
    //   imgEl.src = 'http://www.google.com/s2/favicons?domain=' + new URL(bookmark.url).hostname;

    //   const aEl = document.createElement('a');
    //   aEl.title = bookmark.name;
    //   aEl.href = bookmark.url;
    //   aEl.appendChild(imgEl);
      
    //   div.appendChild(aEl);
    // }
    
  }
